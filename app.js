var myPassword = prompt("Введите пароль")

if (myPassword.toLowerCase() === "пароль") {
    console.log('Пароль введен правильно');
}

// alert(myLogin)
var myLogin = prompt("Введите ЛОГИН")
if (myLogin.toUpperCase() === "ЛОГИН") {
   console.log('Добро пожаловать'); 
}